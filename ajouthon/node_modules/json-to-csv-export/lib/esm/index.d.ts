interface CsvDownloadProps {
    data: any[];
    filename?: string;
    delimiter?: string;
    headers?: string[];
}
declare const csvDownload: ({ data, filename, delimiter, headers, }: CsvDownloadProps) => void;
export default csvDownload;
